{
  "name": "database",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "dependencies": {
    "@prisma/client": "latest"
  },
  "scripts": {
    "db:generate": "npx prisma generate",
    "db:push": "npx prisma db push --skip-generate"
  },
  "devDependencies": {
    "prisma": "^4.8.0"
  }
}
